{
  "name": "multi-agent-generator",
  "version": "1.0.0",
  "main": "dist/scripts/agentGenerator.js",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.198",
    "dotenv": "^16.4.5",
    "fs-extra": "^11.2.0",
    "openai": "^4.104.0",
    "zod": "^3.25.76",
    "zod-to-json-schema": "^3.25.2"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "typescript": "~5.9.3",
    "vitest": "^4.1.9"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "generate-agents": "yarn build && node dist/scripts/agentGenerator.js",
    "compile-rubric": "yarn build && node dist/scripts/rubricCli.js",
    "judge-diff": "yarn build && node dist/scripts/judgeDiff.js",
    "research-pilot": "yarn build && node dist/scripts/researchPilot.js",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
